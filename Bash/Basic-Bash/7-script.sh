#!/bin/bash

mkdir -p projects

touch projects/README.md

#mkdir -p projects: The -p flag ensures that the directory is created only if it doesn't already exist.
#touch projects/README.md: This creates an empty README.md file in the projects directory.
